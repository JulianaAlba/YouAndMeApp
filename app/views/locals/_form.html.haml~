= simple_form_for(@local) do |f|
  = f.error_notification

  .form-inputs
    = f.input :Nomelocal
    = f.input :elevacao
    = f.input :latitude
    = f.input :longitude

  .form-actions
    = f.button :submit
